<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Book</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&family=Lato:wght@400&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

  <div class="card-form">
    <h1>Edit Book</h1>

    <form action="/books/<%= book.id %>/edit" method="POST">
      <label class="form-label">Title</label>
      <input type="text" name="title" class="form-control" value="<%= book.title %>" required>

      <label class="form-label">Author</label>
      <input type="text" name="author" class="form-control" value="<%= book.author %>" required>

      <label class="form-label">Rating (1–5)</label>
      <input type="number" name="rating" min="1" max="5" class="form-control" value="<%= book.rating %>">

      <label class="form-label">Description</label>
      <textarea name="description" class="form-control"><%= book.description %></textarea>

      <label class="form-label">Cover ID (ISBN)</label>
      <small class="form-text">
        Get it from <a href="https://isbnsearch.org/" target="_blank" rel="noopener noreferrer">ISBN Search</a>
      </small>
      <input type="text" name="cover_id" class="form-control" value="<%= book.cover_id %>">

      <label class="form-label">Date Read</label>
      <input type="date" name="date_read" class="form-control" value="<%= book.date_read ? book.date_read.toISOString().split('T')[0] : '' %>">

      <button type="submit">Update Book</button>
    </form>

    <a href="/" class="back-link">← Back</a>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>